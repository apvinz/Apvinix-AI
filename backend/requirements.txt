# TRANSCORE AI — backend dependencies
# Tested on Python 3.10–3.12. See README.md for Windows setup.

fastapi>=0.110,<0.142
uvicorn[standard]>=0.29,<0.53
python-multipart>=0.0.9
aiofiles>=23.2

# Audio analysis / transcription
librosa>=0.10,<2.0
soundfile>=0.12
numpy>=1.24
scipy>=1.11
mido>=1.3

# Score generation
music21>=9.1

# --- Optional (advanced) ---
# Basic Pitch is an OPTIONAL polyphonic transcription engine used only when
# engine="basic_pitch" is explicitly requested. It requires TensorFlow,
# which is only reliably installable on Python 3.10 (NOT 3.11/3.12) at the
# time of writing. Uncomment below only if you set up a Python 3.10
# environment. The default engine ("pyin") needs none of this and works on
# any supported Python version.
# basic-pitch==0.4.0
