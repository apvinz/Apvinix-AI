[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: React Router / client-side routing needs every path to
# resolve to index.html instead of a Netlify 404.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
