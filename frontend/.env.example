# Copy to .env for local overrides, or set this in Netlify's
# Site settings -> Environment variables when deploying to production.
#
# Must point at your deployed backend's /api path, e.g.:
# VITE_API_BASE_URL=https://your-backend.onrender.com/api
VITE_API_BASE_URL=
